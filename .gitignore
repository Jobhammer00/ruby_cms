/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# Logs (RSpec, server; avoid committing secrets-scan noise / false positives)
/log/

# Local security / audit reports (e.g. gitleaks JSON)
/report.json

# rspec failure tracking
.rspec_status
